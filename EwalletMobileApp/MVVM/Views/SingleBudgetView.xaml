<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EwalletMobileApp.MVVM.Views.SingleBudgetView"
             xmlns:helpers="clr-namespace:EwalletMobileApp.Helpers"
             xmlns:viewModels="clr-namespace:EwalletMobileApp.MVVM.ViewModels"
             x:DataType="viewModels:SingleBudgetViewModel">
    
    <Grid BackgroundColor="Black" RowDefinitions="auto,auto,*">
        <Image Grid.Row="0" Source="top_bg.png" Aspect="Fill"/>
        
        <Grid Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <HorizontalStackLayout Grid.Row="0" HeightRequest="35" Grid.Column="0">
                    <Image Source="dotnet_bot.png" HeightRequest="35"></Image>
                    <VerticalStackLayout HorizontalOptions="Center">
                        <Label Text="Welcome back!" FontSize="10"></Label>
                        <Label Text="Hussein Attar"></Label>
                    </VerticalStackLayout>
                </HorizontalStackLayout>

                <Button Background="Transparent" HorizontalOptions="End" Grid.Column="1">
                    <Button.ImageSource>
                        <FontImageSource
                        FontFamily="icomoon"
                        Glyph="{x:Static helpers:IconFont.Bell}"
                        Size="15" Color="Black"/>
                    </Button.ImageSource>
                </Button>
            </Grid>
        </Grid>

        <Frame Grid.Row="1" Margin="15,-45,15,0">
            <Grid RowDefinitions="auto,auto">
                <Label  Text="budget A" HorizontalTextAlignment="Center" Grid.Row="0"  TextColor="Black"/>
                <Label Text="$20.00" HorizontalTextAlignment="Center"  Grid.Row="1" FontSize="22" Grid.Column="0" TextColor="Black"/>
            </Grid>
        </Frame>

        <ScrollView Grid.Row="2" Orientation="Vertical">
            <CollectionView ItemsSource="{Binding Expenses}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid ColumnDefinitions="*,auto" RowDefinitions="auto,*">
                            <Frame BackgroundColor="Red" Grid.ColumnSpan="2" Grid.RowSpan="2" HeightRequest="55" CornerRadius="3"/>
                            <HorizontalStackLayout Grid.Row="0" Grid.Column="1" HorizontalOptions="End">
                                <ImageButton Source="{x:Static helpers:IconFont.Trash}"/>
                                <ImageButton Source="{x:Static helpers:IconFont.Home}"/>
                            </HorizontalStackLayout>

                            <Grid Grid.Row="1" Grid.Column="0" ColumnDefinitions="*,*" RowDefinitions="*,*">
                                <Image Source="dotnet_bot.png" HeightRequest="35" WidthRequest="35" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"/>
                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding CreationDate, StringFormat='{0:MM/dd/yyyy}'}" TextColor="white" FontSize="22"/>
                                <Label Grid.Row="1" Grid.Column="1" Text="{Binding Name}" TextColor="white" FontSize="22"/>
                            </Grid>
                            <Label Grid.Row="1" Grid.Column="1" Text="{Binding Price}" FontSize="Medium" TextColor="white" Margin="0,10,0,0"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>   
        </ScrollView>
    </Grid>
</ContentPage>